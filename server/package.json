{
  "name": "lab2",
  "version": "1.0.0",
  "description": "mean app for bikes ETS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b --verbose && npm run copyviews && npm run copyassets",
    "copyviews": "copyfiles --all ./views/* ./dist",
    "copyassets": "copyfiles --all ./views/assets/* ./dist",
    "start": "node dist/index.js"
  },
  "author": "Equipe 8 GTI525",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "copyfiles": "^2.4.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.3.5",
    "morgan": "^1.10.0",
    "shx": "^0.3.4",
    "typescript": "^4.7.3"
  }
}
